const { MessageEmbed } = require("discord.js");
module.exports = {
  name: "tsarbomba",
  description: "",
  category: "fun",
  run: async (bot, message, args) => {
      message.channel.send(`If you manage to come across this, just know that I gave up on it and may not revisit it.`)
  }
}